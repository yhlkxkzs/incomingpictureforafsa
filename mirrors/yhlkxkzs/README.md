# AFSA 镜像目录 — `yhlkxkzs`

本目录用于接收 **GitHub 用户 `yhlkxkzs`** 通过 AFSA App 写入的样本镜像；
图片与 JSON 与 **您名下收集仓** 中 `summary_images/` 下相对路径一致。

若长期只有本文件而没有 `summary_images/`，常见原因：
- 尚未完成一次「水果种类 + GitHub 通道」识别或状态校准同步；
- 或您的账号对 `yhlkxkzs/incomingpictureforafsa` **没有写权限**（需 Owner 将您加为协作者），此时镜像 PUT 会失败。
